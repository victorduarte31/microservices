server:
  port: 8001
  servlet:
    context-path: /hr-worker

spring:
  application:
    name: hr-worker

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console


